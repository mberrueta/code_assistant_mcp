# Example .envrc file for setting environment variables.

# --- Database Backend Configuration ---
# DB_BACKEND: Specifies the database backend to use.
# Options: "chroma" (default) or "pgvector"
export DB_BACKEND="chroma"
# export DB_BACKEND="pgvector"

# --- ChromaDB Configuration (if DB_BACKEND is "chroma") ---
export CHROMA_HOST="localhost"
export CHROMA_PORT="8000"

# --- PGVector Configuration (if DB_BACKEND is "pgvector") ---
# PG_CONNECTION_STRING: Connection string for PostgreSQL with pgvector.
# Example: postgresql://user:password@host:port/database_name
# export PG_CONNECTION_STRING="postgresql://user:password@localhost:5432/mydatabase"

# --- Embedding Model Configuration ---
# OPENAI_API_KEY: Your OpenAI API key for OpenAIEmbeddings.
# If not set, SentenceTransformerEmbeddings will be used as a fallback for PGVector.
# export OPENAI_API_KEY="your_openai_api_key_here"

[ -f .envrc.custom ] && source .envrc.custom
